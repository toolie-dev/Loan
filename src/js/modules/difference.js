export default class Difference {
    constructor(oldOfficer, newOfficer, items) {
        this.oldOffcer = document.querySelector(oldOfficer);
        this.newOfficer = document.querySelector(newOfficer);
        this.items = items;
    }

    
}